## Summary

Wired the Snake-game example to the shared dual-axis evolution chart from
`common/evolution_chart.ts`. `GenerationInfo` now carries `neurons` and `synapses`, populated from
the champion creature each generation; the runner collects these as `EvolutionSample`s and renders
`docs/screenshots/snake_game/evolution.svg`, embedded in `snake_game/README.md`. Closes #110.

## Evidence

- New deterministic SVG generated by the runner: `docs/screenshots/snake_game/evolution.svg` (best
  score on the left axis, neuron / synapse counts on the right axis).
- Embedded in `snake_game/README.md` under a new "📈 Evolution Chart" section with descriptive
  alt-text.
- `run.sh` now also runs `deno fmt` on the new SVG path so the formatter check stays clean.

```mermaid
flowchart LR
    EVOLVE["evolveSnakeController"] -->|onGeneration { neurons, synapses }| RUNNER
    RUNNER["snake_game.ts main"] -->|push EvolutionSample| SAMPLES["evolutionSamples[]"]
    SAMPLES --> CHART["renderEvolutionChartSVG()"]
    CHART --> SVG["docs/screenshots/snake_game/evolution.svg"]
    SVG --> README["snake_game/README.md"]
```

## Test Plan

- Added `evolveSnakeController emits neurons and synapses on each generation event` in
  `snake_game/snake_game_test.ts` — asserts the new `GenerationInfo` shape (typeof number, expected
  counts `INPUT_COUNT + HIDDEN_COUNT + OUTPUT_COUNT` neurons and `TOTAL_SYNAPSES`).
- Existing snake-game suite (20 tests) continues to pass — including the reproducibility test, so
  the `bestJSON` source for the new fields stays deterministic.
- `deno lint`, `deno fmt --check` (snake_game and the new screenshot directory),
  `deno check **/*.ts`, and the wider unit-test run (232 tests) pass locally.
- `./snake_game/run.sh` completes in ~44s, writes the new chart SVG, and the runner-side `deno fmt`
  of regenerated SVGs stays clean.
